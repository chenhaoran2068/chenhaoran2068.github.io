# Chenhaoran Portal v0.5.1

## Fixed

- Shows exactly one visible language selector at a time. On desktop, `中文 EN 日本語` now appears after search at the far right of the navigation bar.
- Keeps the same selector inside the expanded navigation menu on smaller screens, where the desktop selector is hidden.
- Uses valid single-level navigation list items for the language selector and aligns its responsive breakpoint with the portal navigation breakpoint.
- Updates the official Pages actions to their current Node 24 action runtimes.

## Boundaries

- This is a portal presentation patch only. It does not change any Framework, System, Skill, local runtime, Study, data, account, or external integration.
- The selector remains same-page switching only. It does not use browser-language detection or automatic redirects.
