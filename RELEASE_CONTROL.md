# Portal Release Control

Each portal release identifies the reviewed public release of every listed
component. Updating a component card requires checking its tagged repository
and Release metadata, then rebuilding the portal and passing boundary tests.

The portal has no authority to publish, install, or activate a linked
component. Portal publication, component publication, and local adoption are
separate decisions.
